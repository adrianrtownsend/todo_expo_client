import {
  Box,
  Center,
  FlatList,
  VStack,
  HStack,
  Text,
  Heading,
} from "@gluestack-ui/themed";

const TabList = () => {
  return (
    <VStack flex={1}>
      <HStack></HStack>
    </VStack>
  );
};

export default TabList;
